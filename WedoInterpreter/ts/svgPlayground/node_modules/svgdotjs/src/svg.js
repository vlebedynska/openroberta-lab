import * as svgMembers from 'svg.js'
import { makeInstance } from 'svg.js/src/utils/adopter.js'

// The main wrapping element
export default function SVG (element) {
  return makeInstance(element)
}

Object.assign(SVG, svgMembers)
